<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Posts</title>
</head>
<body>
  <h1>Hello World</h1>
  <h3>Here are the posts:</h3>
  <% @posts.each.with_index(1) do |post, index| %>
  <p><%= "#{index}. #{post.name}" %></p>
  <div class="indented">
    <ul>
      <li><%= "#{post.content}" %></li>
    </ul>
  </div>
</body>
</html>
<% end %>
